services:
  - type: web
    name: football-predictor-backend
    runtime: python
    pythonVersion: "3.8.16"  # Explicitly specify Python 3.8
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
