services:
  - type: web
    name: football-predictor-backend
    runtime: docker
    dockerfilePath: Dockerfile
    buildCommand: docker build -t football-predictor .
    startCommand: docker run -p 5000:5000 football-predictor
