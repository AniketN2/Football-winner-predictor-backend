services:
  - type: web
    name: football-predictor-backend
    runtime: python
    pythonVersion: "3.9.13"  # Must use Python 3.9
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: gunicorn app:app
